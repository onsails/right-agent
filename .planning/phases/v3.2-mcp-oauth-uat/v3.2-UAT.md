---
status: diagnosed
phase: v3.2-mcp-oauth
source: [32-01-SUMMARY.md, 33-01-SUMMARY.md, 34-01-SUMMARY.md, 34-02-SUMMARY.md, 34-03-SUMMARY.md, 34-04-SUMMARY.md, 35-01-SUMMARY.md, 35-02-SUMMARY.md, 35-03-SUMMARY.md, 37-01-SUMMARY.md, 37-02-SUMMARY.md, 37-03-SUMMARY.md]
started: 2026-04-04T11:00:00Z
updated: 2026-04-04T12:00:00Z
---

## Current Test

[testing paused — 2 tests blocked on missing `rightclaw mcp` CLI subcommand]

## Tests

### 1. rightclaw init --tunnel-token saves config
expected: Run `rightclaw init --tunnel-token <some-token>`. Command succeeds. Inspect `~/.rightclaw/config.yaml` — it should contain `tunnel.token` and `tunnel.hostname` (hostname derived from JWT or set via --tunnel-hostname). Running `rightclaw init` again with a different token should overwrite it.
result: pass
notes: tunnel.token and tunnel.hostname (right.onsails.me) both present. Hostname is not UUID — correctly derived.

### 2. rightclaw doctor — cloudflared binary check
expected: Run `rightclaw doctor`. If cloudflared is not installed, a Warn entry appears: "cloudflared binary not found" with an install suggestion. If it is installed, the check shows Pass.
result: pass

### 3. rightclaw doctor — tunnel config check
expected: Run `rightclaw doctor`. If no tunnel has been configured (no `rightclaw init --tunnel-token`), a Warn entry appears: "tunnel not configured" with `rightclaw init --tunnel-token` as the fix command. After running init with a token, re-running doctor shows Pass for both token and hostname fields.
result: pass
notes: tunnel-config ok, tunnel-token ok (UUID shown)

### 4. rightclaw doctor — mcp-tokens check
expected: Run `rightclaw doctor`. A "mcp-tokens" check is present. If all agent tokens are valid (or non-expiring), it shows Pass. If any token is expired or missing, it shows Warn listing the agent/server pair. Tokens with expires_at=0 are treated as non-expiring (Pass).
result: pass

### 5. Telegram /mcp add and /mcp remove
expected: Send `/mcp add notion https://mcp.notion.com/mcp`. Bot confirms server added. Send `rightclaw mcp status` and the server appears with its auth state. Then send `/mcp remove notion` — bot confirms removal, server disappears from status.
result: issue
reported: "/mcp add dispatched (logs show mcp add agent_dir=...) but no completion log. rightclaw mcp status — unrecognized subcommand 'mcp' — CLI mcp subcommand missing entirely."
severity: major

### 6. rightclaw mcp status (all agents)
expected: Run `rightclaw mcp status`. Should print a table grouped by agent showing each HTTP MCP server with its auth state: OK (token present + valid), no token / auth required (no token), or EXPIRED (token past expires_at). Stdio-only agents produce no output (skipped silently).
result: blocked
blocked_by: other
reason: "rightclaw mcp subcommand missing from CLI — unrecognized subcommand 'mcp'"

### 7. rightclaw mcp status --agent <name>
expected: Run `rightclaw mcp status --agent <some-agent>`. Output filtered to that single agent only. Unknown agent name returns an error.
result: blocked
blocked_by: other
reason: "rightclaw mcp subcommand missing from CLI — unrecognized subcommand 'mcp'"

### 8. rightclaw up warns about unauthenticated servers
expected: Run `rightclaw up` with at least one agent that has an HTTP MCP server with no token. Before process-compose launches, a warning line is visible to user (via eprintln! or similar, not lost to TUI) naming the agent/server pairs that are missing auth.
result: issue
reported: "Pre-launch warning (if present) is invisible — TUI starts immediately and overwrites it. Bot startup logs show no MCP token warning either — nothing between set_my_commands and dispatcher starting."
severity: minor

### 9. Telegram /mcp list
expected: Send `/mcp list` in Telegram. Bot replies with a formatted table showing each MCP server per agent with icon: ✅ (OK), ❌ (no token), ⚠️ (EXPIRED). If no HTTP MCP servers configured, bot replies gracefully.
result: issue
reported: "Bot shows 'MISSING' label instead of 'no token' or 'auth required'. Icons not shown."
severity: minor

### 10. Telegram /mcp auth — OAuth flow initiation
expected: Send `/mcp auth notion` (or any server missing auth). Bot responds with an OAuth authorization URL to open in browser. URL contains PKCE challenge parameters. Cloudflared tunnel is running and the callback URL uses `tunnel.hostname` from config (not the UUID cfargotunnel.com hostname). No "healthcheck failed" error.
result: issue
reported: "OAuth URL generated, tunnel works (right.onsails.me resolves), Notion auth completed — but callback returns 404. URL: https://right.onsails.me/oauth/right/callback?code=...&state=... — callback handler not handling this path."
severity: major

### 11. Telegram /doctor
expected: Send `/doctor` in Telegram. Bot replies with a formatted summary of doctor checks including the cloudflared and mcp-tokens checks. Pass count shown. Checks match `rightclaw doctor` CLI output.
result: issue
reported: "Output is correct and matches CLI, but not wrapped in ``` code block — hard to read in Telegram."
severity: cosmetic

## Summary

total: 11
passed: 4
issues: 5
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "rightclaw mcp status shows per-agent HTTP MCP server auth state"
  status: failed
  reason: "User reported: rightclaw mcp status — unrecognized subcommand 'mcp'. CLI mcp subcommand missing entirely."
  severity: major
  test: 5
  root_cause: "McpCommands enum, Commands::Mcp variant, and cmd_mcp_status were added in commit e0db55d (feat 33-01) but dropped in a817e0c (fix 37) — 1113-line rewrite of main.rs silently removed them. Library-side API (mcp::detect::mcp_auth_status) is intact."
  artifacts:
    - path: "crates/rightclaw-cli/src/main.rs"
      issue: "missing McpCommands enum, Commands::Mcp variant, dispatch arm, and cmd_mcp_status function"
  missing:
    - "Re-add McpCommands enum with Status { agent: Option<String> } variant"
    - "Wire Mcp { command: McpCommands } into Commands enum"
    - "Restore cmd_mcp_status function calling rightclaw::mcp::detect::mcp_auth_status"

- truth: "rightclaw up MCP warning visible to user before TUI launches"
  status: failed
  reason: "User reported: pre-launch warning invisible — TUI starts immediately. Bot startup logs show no MCP token warning between set_my_commands and dispatcher starting."
  severity: minor
  test: 8
  root_cause: "eprintln! IS present in main.rs line 600 but fires just before cmd.status().await (line 670) which hands terminal to process-compose TUI — warning is immediately overwritten. Bot lib.rs has zero MCP token validation at startup — never implemented."
  artifacts:
    - path: "crates/rightclaw-cli/src/main.rs"
      issue: "eprintln! at line 600 gets clobbered by TUI takeover at line 670"
    - path: "crates/bot/src/lib.rs"
      issue: "no MCP auth check anywhere in run_async startup path"
  missing:
    - "Write MCP warning to persistent log file OR delay process-compose launch briefly so warning is visible"
    - "Add mcp_auth_issues check in bot lib.rs run_async between delete_webhook and dispatcher start, emit tracing::warn! per missing server"

- truth: "Telegram /mcp list shows auth state icons and clear labels"
  status: failed
  reason: "User reported: bot shows 'MISSING' label instead of 'no token'/'auth required'. No icons (✅/❌/⚠️)."
  severity: minor
  test: 9
  root_cause: "handle_mcp_list in handler.rs lines 261-266 uses hardcoded plain strings 'OK'/'MISSING'/'EXPIRED' and formats as 'name -- STATUS' — no emoji. AuthState::Display impl ('present'/'auth required'/'expired') exists in detect.rs but is never called."
  artifacts:
    - path: "crates/bot/src/telegram/handler.rs"
      issue: "icon match at lines 261-265 uses plain ASCII strings, format string has no icon slot"
  missing:
    - "Replace icon match with ✅/❌/⚠️ emoji"
    - "Use AuthState Display impl or new emoji helper in format string"

- truth: "OAuth callback handled — token exchanged and stored after Notion auth"
  status: failed
  reason: "User reported: Notion auth completed but callback returns 404 at https://right.onsails.me/oauth/right/callback — callback handler not reached."
  severity: major
  test: 10
  root_cause: "cloudflared-start.sh runs 'cloudflared tunnel run --token TOKEN' without '--config <path>'. Without --config, cloudflared ignores the generated cloudflared-config.yml (which has correct ingress rules routing /oauth/right/callback to the unix socket). All requests hit cloudflared's implicit 404."
  artifacts:
    - path: "crates/rightclaw-cli/src/main.rs"
      issue: "script template at line 569 missing '--config {cf_config_path}' flag"
    - path: "crates/rightclaw/src/codegen/cloudflared.rs"
      issue: "config generated correctly but never passed to cloudflared process"
  missing:
    - "Add '--config {cf_config_path}' to cloudflared tunnel run in script template at main.rs line 569"

- truth: "Telegram /doctor output wrapped in code block for readability"
  status: failed
  reason: "User reported: output correct and matches CLI but not wrapped in ``` code block — hard to read in Telegram."
  severity: cosmetic
  test: 11
  root_cause: "handle_doctor in handler.rs line 625-634 builds plain text string and sends via send_message without Markdown/HTML formatting. No code block wrapping."
  artifacts:
    - path: "crates/bot/src/telegram/handler.rs"
      issue: "line 625: String::from('Doctor results:\n\n') — no ``` wrapping; line 634: send_message without parse_mode"
  missing:
    - "Wrap output in ``` block and set parse_mode(MarkdownV2) or use pre-formatted text"
